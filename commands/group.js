'use strict';
const kick = async (ctx) => { if (!ctx.isGroup) return ctx.reply('❌ Group only!'); const m = ctx.msg.message?.extendedTextMessage?.contextInfo?.participant; if (!m) return ctx.reply('❌ Reply to a message!'); await ctx.sock.groupParticipantsUpdate(ctx.from, [m], 'remove'); await ctx.react('✅'); };
const add = async (ctx) => { if (!ctx.isGroup) return ctx.reply('❌ Group only!'); const num = ctx.args[0]?.replace(/[^0-9]/g,''); if (!num) return ctx.reply('❌ Provide number!'); await ctx.sock.groupParticipantsUpdate(ctx.from, [num+'@s.whatsapp.net'], 'add').catch(e => ctx.reply('❌ '+e.message)); await ctx.react('✅'); };
const promote = async (ctx) => { if (!ctx.isGroup) return ctx.reply('❌ Group only!'); const m = ctx.msg.message?.extendedTextMessage?.contextInfo?.participant; if (!m) return ctx.reply('❌ Reply!'); await ctx.sock.groupParticipantsUpdate(ctx.from, [m], 'promote'); await ctx.react('✅'); };
const demote = async (ctx) => { if (!ctx.isGroup) return ctx.reply('❌ Group only!'); const m = ctx.msg.message?.extendedTextMessage?.contextInfo?.participant; if (!m) return ctx.reply('❌ Reply!'); await ctx.sock.groupParticipantsUpdate(ctx.from, [m], 'demote'); await ctx.react('✅'); };
const mute = async (ctx) => { if (!ctx.isGroup) return ctx.reply('❌ Group only!'); await ctx.sock.groupSettingUpdate(ctx.from, 'announcement'); await ctx.reply('🔇 Group muted!'); };
const unmute = async (ctx) => { if (!ctx.isGroup) return ctx.reply('❌ Group only!'); await ctx.sock.groupSettingUpdate(ctx.from, 'not_announcement'); await ctx.reply('🔊 Group unmuted!'); };
const tagall = async (ctx) => { if (!ctx.isGroup) return ctx.reply('❌ Group only!'); const meta = await ctx.sock.groupMetadata(ctx.from); const mentions = meta.participants.map(p => p.id); const text = mentions.map(j => '@'+j.split('@')[0]).join(' '); await ctx.sock.sendMessage(ctx.from, { text, mentions }, { quoted: ctx.msg }); };
const hidetag = async (ctx) => { if (!ctx.isGroup) return ctx.reply('❌ Group only!'); const meta = await ctx.sock.groupMetadata(ctx.from); const mentions = meta.participants.map(p => p.id); await ctx.sock.sendMessage(ctx.from, { text: ctx.args.join(' ') || 'Notice', mentions }, { quoted: ctx.msg }); };
const groupinfo = async (ctx) => { if (!ctx.isGroup) return ctx.reply('❌ Group only!'); const meta = await ctx.sock.groupMetadata(ctx.from); await ctx.reply('📋 *Group Info*\n\nName: '+meta.subject+'\nMembers: '+meta.participants.length+'\nDesc: '+(meta.desc||'None')); };
const setname = async (ctx) => { if (!ctx.isGroup) return ctx.reply('❌ Group only!'); if (!ctx.isOwner) return ctx.reply('❌ Owner only!'); const name = ctx.args.join(' '); if (!name) return ctx.reply('❌ Provide name!'); await ctx.sock.groupUpdateSubject(ctx.from, name); await ctx.react('✅'); };
const setdesc = async (ctx) => { if (!ctx.isGroup) return ctx.reply('❌ Group only!'); if (!ctx.isOwner) return ctx.reply('❌ Owner only!'); const desc = ctx.args.join(' '); if (!desc) return ctx.reply('❌ Provide desc!'); await ctx.sock.groupUpdateDescription(ctx.from, desc); await ctx.react('✅'); };
const linkgc = async (ctx) => { if (!ctx.isGroup) return ctx.reply('❌ Group only!'); const code = await ctx.sock.groupInviteCode(ctx.from); await ctx.reply('🔗 https://chat.whatsapp.com/'+code); };
const revokegc = async (ctx) => { if (!ctx.isGroup) return ctx.reply('❌ Group only!'); if (!ctx.isOwner) return ctx.reply('❌ Owner only!'); await ctx.sock.groupRevokeInvite(ctx.from); await ctx.react('✅'); };
const antilink = async (ctx) => { const db = require('../database/db'); const val = ctx.args[0]?.toLowerCase(); if (!val) return ctx.reply('Use: .antilink on/off'); db.setAntiLink(ctx.cleanBotNum, ctx.from, val==='on'); await ctx.reply('Antilink: '+(val==='on'?'✅ ON':'❌ OFF')); };
module.exports = { kick, add, promote, demote, mute, unmute, tagall, hidetag, groupinfo, setname, setdesc, linkgc, revokegc, antilink };
